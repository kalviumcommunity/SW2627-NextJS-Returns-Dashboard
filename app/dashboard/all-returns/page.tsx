import ReturnsList from "@/components/ReturnsList";

export default function AllReturnsPage() {
  return (
    <ReturnsList
      title="All Returns"
      subtitle="Every return request across every status"
    />
  );
}
